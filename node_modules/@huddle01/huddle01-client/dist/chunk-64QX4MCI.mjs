import {
  createPortSlice_default
} from "./chunk-S7QTJVEH.mjs";
import {
  createTestSlice_default
} from "./chunk-B6M7DWMW.mjs";
import {
  createViewPortSlice_default
} from "./chunk-6YZSOUNT.mjs";

// src/store/portStore/usePortStore.ts
import create from "zustand";
import { devtools } from "zustand/middleware";
var createPortStore = (set, get) => ({
  ...createTestSlice_default(set, get),
  ...createPortSlice_default(set, get),
  ...createViewPortSlice_default(set, get)
});
var usePortStore = create(
  devtools(createPortStore, {
    name: "huddle01-PortStore",
    anonymousActionType: "action"
  })
);
var { getState, setState } = usePortStore;
var usePortStore_default = usePortStore;

export {
  getState,
  setState,
  usePortStore_default
};
