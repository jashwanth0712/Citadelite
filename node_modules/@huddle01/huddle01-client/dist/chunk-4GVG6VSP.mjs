// src/store/createHostControlsSlice.ts
var createHostControlsSlice = (set, get) => ({
  hostId: null,
  hostState: "host-true",
  meId: "",
  coHosts: [],
  hostControl: {
    allowVideo: true,
    allowAudio: true,
    allowScreenShare: true,
    allowChat: true
  },
  setHost: (hostId) => {
    set(() => ({ hostId }));
  },
  setCoHosts: (coHosts) => {
    set(() => ({ coHosts }));
  },
  setHostState: (hostState) => {
    set(() => ({ hostState }));
  },
  setHostControl: (control) => {
    set(() => ({ hostControl: control }));
  },
  setSingleHostControl: (control, val) => {
    set(() => ({
      hostControl: { ...get().hostControl, [control]: val }
    }));
  },
  initHostControls: (hostId, meId, hostControl, coHosts) => {
    set(() => ({ hostId, meId, coHosts, hostControl }));
  }
});
var createHostControlsSlice_default = createHostControlsSlice;

export {
  createHostControlsSlice_default
};
