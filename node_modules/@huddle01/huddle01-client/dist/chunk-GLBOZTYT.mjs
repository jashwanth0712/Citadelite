import {
  useHuddleStore
} from "./chunk-DM7RIPEQ.mjs";

// src/hooks/useMeCamTrack.ts
import { useState } from "react";
var useMeCamTrack = () => {
  const [error, setError] = useState(null);
  const meCamStream = useHuddleStore((state) => state.producers.cam);
  if (!meCamStream) {
    setError("No stream found");
    return {
      error
    };
  }
  const { track, appData, id, isPaused } = meCamStream;
  const data = {
    id,
    isPaused,
    appData
  };
  return {
    track,
    data
  };
};
var useMeCamTrack_default = useMeCamTrack;

export {
  useMeCamTrack_default
};
