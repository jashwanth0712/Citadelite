import {
  logger_default
} from "./chunk-IBGL6YYT.mjs";

// src/store/createLayoutSlice.ts
var createLayoutSlice = (set, get) => ({
  layout: {
    type: "sideBarView"
  },
  setLayoutView: (type) => {
    if (get().layout.type === type) {
      logger_default.warn("Layout type is already set to: ", type);
      return;
    }
    set(() => ({ layout: { type } }));
  }
});
var createLayoutSlice_default = createLayoutSlice;

export {
  createLayoutSlice_default
};
