import { ProtooUrlData } from '../../schema/index.js';
import { Device, types } from 'mediasoup-client';
import { RoomSocketClient } from './RoomSocketClient.js';
import '../../store/store.js';
import 'zustand';
import '../../store/storeTypes.js';
import '../../schema/enums/EHostControlType.js';
import '../../store/portStore/usePortStore.js';
import './HuddleStore.js';
import 'protoo-client';

declare class RoomMediasoupClient extends RoomSocketClient {
    mediasoupDevice: null | Device;
    sendTransport: null | types.Transport;
    recvTransport: null | types.Transport;
    consumers: Map<string, types.Consumer>;
    _turn: RTCIceServer[];
    constructor(apiKey: ProtooUrlData['apiKey']);
    createMediasoupTransport(producing: boolean, consuming: boolean): Promise<any>;
    createSendTransport(): Promise<void>;
    createRecvTransport(): Promise<void>;
    loadMediasoupDevice(): Promise<void>;
}

export { RoomMediasoupClient };
