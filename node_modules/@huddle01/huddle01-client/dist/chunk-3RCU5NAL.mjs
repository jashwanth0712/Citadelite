import {
  useHuddleStore
} from "./chunk-DM7RIPEQ.mjs";

// src/hooks/usePeerCamTrack.ts
import { useState } from "react";
var usePeerCamTrack = (peerId) => {
  const [error, setError] = useState(null);
  const peerCamStream = useHuddleStore(
    (state) => {
      var _a;
      return (_a = state.peers[peerId].consumers) == null ? void 0 : _a.cam;
    }
  );
  if (!peerCamStream) {
    setError("No stream found");
    return {
      error
    };
  }
  const { track, appData, id, isPaused } = peerCamStream;
  const data = {
    id,
    isPaused,
    appData
  };
  return {
    track,
    data
  };
};
var usePeerCamTrack_default = usePeerCamTrack;

export {
  usePeerCamTrack_default
};
