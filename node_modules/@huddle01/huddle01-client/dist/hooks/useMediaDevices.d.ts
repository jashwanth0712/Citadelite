declare const useMediaDevices: (constraints?: MediaStreamConstraints) => {
    stream: MediaStream | null;
    loading: boolean;
    getMediaDevices: () => Promise<MediaDeviceInfo[]>;
    pauseTracks: () => void;
};

export { useMediaDevices as default };
