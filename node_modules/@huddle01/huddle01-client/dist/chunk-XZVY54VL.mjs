import {
  HuddleClient
} from "./chunk-V6FRJADG.mjs";

// src/HuddleClient/hc.ts
import randomString from "random-string";
var hc;
var createHuddleClient = (apiKey, isBot = false) => {
  const peerId = isBot ? "recorder_bot" : randomString({ length: 10 });
  hc = new HuddleClient(peerId, isBot, apiKey);
  console.log({ hc });
  return hc;
};
var getHuddleClient = (apiKey, isBot = false) => {
  if (!hc)
    createHuddleClient(apiKey, isBot);
  return hc;
};

export {
  createHuddleClient,
  getHuddleClient
};
